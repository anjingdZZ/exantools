# 考公错题整理助手 — 方案文档

> AI 驱动的错题整理工具：拍照 → 结构化知识笔记 → 同步 Notion

---

## 一、产品定位

### 目标用户
考公备考人群（行测为主），解决以下痛点：

| 痛点 | 现状 | 本方案 |
|------|------|--------|
| 错题散落在各 App / 纸质卷子 / 截图相册 | 无法统一查阅 | 集中到 Notion 一个地方 |
| 复盘时只看到图片，没有结构化信息 | 翻相册回忆成本高 | 每道题有知识点/错误原因/解题思路 |
| 找不到同类错题 | 靠脑记 | Notion 视图筛选一键聚合 |
| 不知道薄弱点在哪 | 凭感觉 | 按错误原因/知识点统计可视化 |

### 核心价值
**拍一张图，Notion 里就多一道完整可复习的错题。**

---

## 二、整体架构

```
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│                   │     │                   │     │                   │
│  用户界面          │     │  后端服务          │     │  Notion           │
│  (Web / 小程序)    │────▶│  (API 处理中心)    │────▶│  (知识库)         │
│                   │     │                   │     │                   │
│  - 上传/拖拽图片    │     │  - OCR/图片解析    │     │  - 错题数据库      │
│  - 预览识别结果     │     │  - LLM 结构化      │     │  - 按模块视图      │
│  - 编辑确认         │     │  - Notion API 写入 │     │  - 按错误原因视图   │
│  - 一键同步         │     │  - 错题去重         │     │  - 按知识点视图    │
│                   │     │                   │     │                   │
└──────────────────┘     └──────────────────┘     └──────────────────┘

数据流:

  拍照/截图 → 上传图片 → [图片预处理] → [LLM Vision] → [结构化 JSON]
                                                          ↓
  用户确认/编辑 ←────────── 展示结果 ←────────────────────┘
       ↓
  [同步 Notion] → 创建/更新 Page → 表格视图可见 → 点开查看详情
```

---

## 三、技术选型

### 3.1 需求约束

从使用场景倒推技术选型：

| 需求 | 含义 |
|------|------|
| **手机可操作** | 前端必须在手机浏览器流畅运行，原生拍照/选图 |
| **拍照上传** | 调用系统相机，拍完即传 |
| **单人/小范围使用** | 不需要复杂的用户系统，不需要上架应用商店 |
| **快速验证** | MVP 阶段希望尽快跑通全链路，再迭代 |

### 3.2 方案对比

| 方案 | 手机拍照 | 开发速度 | 需审核 | 维护成本 | 推荐度 |
|------|---------|---------|-------|---------|--------|
| **PWA 网页** ⭐ | ⭐⭐⭐⭐ 浏览器调用相机 | ⭐⭐⭐⭐⭐ | ❌ 不需要 | 低 | **首选** |
| **微信小程序** | ⭐⭐⭐⭐⭐ 原生拍照体验 | ⭐⭐ 需注册/审核/工具链 | ✅ 需要 | 中 | 迭代期考虑 |
| **Telegram Bot** | ⭐⭐⭐ 发图给机器人 | ⭐⭐⭐⭐⭐ | ❌ 不需要 | 低 | 备选 |
| **Flutter / RN 原生** | ⭐⭐⭐⭐⭐ | ⭐ 太重 | ✅ 需要 | 高 | 不推荐 |

### 3.3 推荐方案：PWA（渐进式 Web 应用）

**为什么选 PWA：**

1. **手机浏览器直接打开** — 拍照用 `<input capture="environment">`，和原生 App 体验几乎一样
2. **可以添加到桌面** — 在 Safari 点「添加到主屏幕」，图标+全屏，看着就是个 App
3. **前后端一套代码** — 前端 HTML+JS，后端一个服务，一个人两天搞定
4. **不用审核** — 改完刷新就能用，不像小程序发版要等审核
5. **Mac 上开发，手机上调试** — VSCode 写完，终端启动服务，同一 WiFi 下手机直接访问

**局限性：**
- iOS Safari 的 PWA 后台刷新不如原生稳定（但错题整理不是实时场景，不影响）
- 无法调用系统通知（暂时不需要）

### 3.4 推荐技术栈

```
前端:    HTML + Tailwind CSS (CDN) + 原生 JS
         — 零构建，直接写，手机浏览器打开就用

后端:    Python FastAPI 或 Node.js Express
         — 选你更熟的语言，逻辑很简单就一个接口

LLM:     Claude API
         — Vision 直接读图，无需单独 OCR，中文理解最好

图片存储: 第一阶段 base64 直接发，不落盘
          第二阶段加 Cloudflare R2（免费额度大，免审核）

Notion:   @notionhq/client（官方 SDK）
```

**技术选型细节：**

| 层级 | MVP 方案 | 成熟期方案 |
|------|---------|-----------|
| 前端 | HTML + Tailwind CDN + JS | 可升级为 React/Vue |
| 后端 | Python FastAPI 或 Node.js Express | 同上，加缓存层 |
| 部署 | 本地开发机 / Vercel 免费层 | 自家服务器 / Railway |
| 图片处理 | 直接传 base64 调 API | 上传到 R2 再处理 |
| 数据库 | 不必要 | SQLite / Supabase（去重用） |
| PWA 清单 | 手动写 manifest.json | 同上，无需升级 |

### 3.5 LLM 选型对比

| 模型 | 图片理解 | 结构化能力 | 成本 | 速度 |
|------|---------|-----------|------|------|
| Claude Sonnet 4.6 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 中等 | 快 |
| GPT-4o | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 中等 | 快 |
| Claude Haiku | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 低 | 最快 |

**推荐起点：Claude API** — 对中文题目理解最好，结构化输出稳定，可直接要求输出 JSON。

### 3.6 本地开发调试流程

```bash
# 在 Mac 上开发
cd wrong-answer-assistant/
code .                        # VSCode 写代码

# 本地启动服务
python main.py 或 npm run dev  # 监听 localhost:3000

# 手机打开测试
# 同一 WiFi 下，手机浏览器访问 http://你Mac的IP:3000
# 就能直接拍照上传调试了
```

---

## 四、知识点分类体系设计

### 4.1 设计思路：参照花生十三的解题思维体系

常规的错题分类是按题目表象分的（如「加强题」「削弱题」），但**花生十三体系的精髓在于按解题思维过程分类**。这样分类的好处是：复盘时看到的不是「这道题是什么」，而是「我当时应该怎么想」。

| 传统分类 | 花生体系分类 | 复盘价值 |
|---------|------------|---------|
| 削弱论证 | **归因论证 / 类比论证 / 一般质疑 / 方案论证** | 削弱题有四种不同的削弱逻辑，混为一谈无法针对性提高 |
| 图形推理：按考点平铺 | **先定特征→再想规律**（组成相同→位置；相似→样式；不同→属性/数量） | 考场第一步不是想规律，而是定特征，按特征想对应规律 |
| 资料分析：按概念分 | **按速算策略分**（直除/特征数字/分数比较/估算/假设分配） | 错题是因为「不知道用什么方法」，不是「不理解概念」 |
| 逻辑判断 | **形式推理 / 论证推理**两大分支，论证再按模型细分 | 形式推理是公式推导，论证是逻辑模型，需要不同的处理方式 |

### 4.2 完整分类预设表

以下分类参照花生十三体系设计，贯穿入 Prompt，确保 LLM 输出稳定且对复盘有实际指导意义。

#### 判断推理

```
┌─ 图形推理（按图形特征定解题方向）
│   ├── 组成相同 → 位置规律（平移/旋转/翻转）
│   ├── 相似 → 样式规律（遍历/叠加/黑白运算/求同存异）
│   ├── 组成不同（属性）→ 对称/曲直/开闭
│   └── 组成不同（数量）→ 面/线/点/角/元素/笔画
│
├─ 定义判断（关键词提取）
│   └── 主体/客体/条件/方式/目的/结果/原因
│
├─ 类比推理（关系判定）
│   ├── 语义关系（近义/反义/象征/比喻）
│   ├── 语法关系（主谓/动宾/偏正）
│   └── 逻辑关系（全同/并列/包容/交叉/对应）
│
└─ 逻辑判断
    ├── 形式推理（翻译推理/真假推理/排列组合/集合推理）
    └── 论证推理（花生核心模型）
        ├── 归因论证 ── 另有他因 / 因果倒置 / 否定此因
        ├── 类比论证 ── 类比对象是否可比
        ├── 一般质疑 ── 直接反驳论点 / 拆桥
        ├── 方案论证 ── 方案可行性 / 方案副作用
        ├── 前提假设 ── 搭桥 / 必要条件
        └── 推出型 ─── 从题干能推出什么
```

#### 言语理解

```
┌─ 逻辑填空
│   ├── 语境分析（转折/递进/并列/解释说明/因果关系）
│   └── 词语辨析（实词/成语/关联词）
│
├─ 片段阅读
│   ├── 主旨概括（转折/递进/结论/必要条件/并列/行文脉络）
│   ├── 意图判断（寓言/故事/观点/对策）
│   ├── 细节理解（细节判断/细节查找）
│   └── 词句理解（代词指代/语句理解）
│
├─ 语句表达
│   ├── 语句排序（首尾句判定/关联词/代词/顺序词）
│   └── 语句填空（承上启下/总领/总结）
│
└─ 篇章阅读（综合题组，按以上题型混合）
```

#### 数量关系

```
┌─ 数学运算
│   ├── 工程问题（赋值法/方程法）
│   ├── 行程问题（相遇/追及/流水行船/火车过桥）
│   ├── 经济利润（基础公式/分段计费/最值优化）
│   ├── 排列组合（分类/分步/捆绑/插空/隔板）
│   ├── 概率问题（古典概型/条件概率/几何概型）
│   ├── 容斥问题（两集合/三集合/画图法）
│   ├── 几何问题（平面/立体/割补/坐标）
│   ├── 最值问题（抽屉原理/构造数列/反向构造）
│   ├── 溶液问题（浓度/混合/十字交叉）
│   ├── 年龄问题（年龄差不变/方程法）
│   ├── 牛吃草（核心公式/方程法）
│   ├── 植树问题（线性/环形/楼间）
│   ├── 方阵问题（空心/实心）
│   ├── 周期问题（余数/循环）
│   └── 钟表问题（角度/追及）
│
└─ 数字推理（考得少但仍存在）
    ├── 基础数列（等差/等比/质数/平方/立方）
    ├── 递推数列（和/差/积/商/倍）
    └── 特殊数列（分数/根号/小数/数位组合）
```

#### 资料分析

```
┌─ 核心概念
│   ├── 增长率（普通增长率/间隔增长率/混合增长率/年均增长率）
│   ├── 比重（现期比重/基期比重/比重变化）
│   ├── 倍数与平均数
│   └── 增长量计算与比较
│
├─ 速算技巧（花生体系核心）
│   ├── 直除法
│   ├── 特征数字法
│   ├── 分数比较法
│   ├── 估算（截位/放缩）
│   ├── 假设分配法（花生招牌）
│   └── 415 法
│
└─ 做题策略
    ├── 读题圈圈法（花生招牌）
    └── 常见陷阱（时间陷阱/单位陷阱/概念混淆）
```

#### 常识判断

```
┌─ 法律（宪法/行政法/民法/刑法/新法）
├─ 科技（物理/化学/生物/生活常识）
├─ 文史（中国史/文化/文学/哲学）
├─ 地理（自然/人文/地图）
├─ 经济（宏观经济/微观经济/经济政策）
├─ 时政（重要会议/政策文件/科技成就）
└─ 管理（行政管理/公文）
```

### 4.3 知识点预设策略

| 字段 | 预设策略 | 依据 |
|------|---------|------|
| **模块** | 完全预设（5 个） | 行测模块不会变 |
| **题型** | 完全预设（按上方分类） | 参照花生体系，题型体系成熟稳定 |
| **错误原因** | 完全预设（6 个） | 知识点盲区 / 审题失误 / 掉坑 / 计算错误 / 时间不够 / 思路卡壳 |
| **知识点** | 预设 80% + LLM 补充 20% | 主体分类固定，但 LLM 可补充细颗粒度子标签 |
| **来源** | 常见来源预设 + 自由填写 | 粉笔 / 华图 / 花生十三 / 中公 / 历年真题 / 其他 |

Prompt 中的知识点字段按上述树形结构组织，LLM 优先从预设中选择，如确无匹配则允许新增，新增标签会被记录用于后续扩充预设列表。

---

## 五、Notion 数据库设计

### 4.1 Database 属性定义

在 Notion 中创建一个 Database，属性如下：

| 属性名 | 类型 | 用途 | 示例 |
|--------|------|------|------|
| `题目编号` | 公式 (auto) | 唯一标识 | WT-0001 |
| `题目摘要` | Title | 简短描述，表格中可见 | 对称轴方向+旋转45°规律 |
| `模块` | Select | 行测五大模块 | 判断推理 |
| `题型` | Select | 具体题型 | 图形推理 / 逻辑填空 / 工程问题 |
| `错误原因` | Select | 复盘归类 | 知识点盲区 / 审题失误 / 掉坑 / 计算错误 |
| `知识点` | Multi-select | 关联知识点标签 | 对称轴旋转 / 转折词递进 |
| `我的答案` | Text | 记录当时选了什么 | B |
| `正确答案` | Text | 标准答案 | C |
| `来源` | Select | 题目出处 | 2024 国考 / 粉笔模考 / 花生十三 |
| `错题次数` | Number | 去重累计 | 2 |
| `上次错误日期` | Date | 最近的错误时间 | 2025-03-20 |
| `掌握状态` | Select | 复习进度 | 待复习 / 基本掌握 / 已掌握 |
| `原图` | Files & Media | 原始错题照片 | (图片链接) |

### 4.2 Page 内容结构

每个 Database Item 点开是一个完整的 Page，内容由 **Block** 组成：

```
┌────────────────────────────────────────────────┐
│  [标题] 2024 国考 · 判断推理 · 图形推理       │
│  [属性面板] 模块 / 题型 / 错误原因 / 知识点...  │
│                                                │
│  ─── 原题 ───                                  │
│  [图片块] 原始错题照片                         │
│  [文本块] 从所给的四个选项中...                 │
│                                                │
│  ─── 选项 ───                                  │
│  [文本块] A. □□□   B. □□□                     │
│           C. □□□   D. □□□                     │
│                                                │
│  ─── 解析 ───                                  │
│  [文本块] 解题思路 (AI 生成 + 可编辑)          │
│                                                │
│  ─── 我的笔记 ───                              │
│  [文本块] 用户自行补充 (预留空白)              │
└────────────────────────────────────────────────┘
```

### 4.3 预设视图

在 Notion 中创建多个 View，方便不同维度复习：

| 视图 | 筛选条件 | 排序 | 用途 |
|------|---------|------|------|
| **全部错题** | 无 | 最近错误日期 ↓ | 全局浏览 |
| **按模块** | 分组：模块 | — | 模块专项突破 |
| **按错误原因** | 分组：错误原因 | — | 发现共性错误 |
| **高频错题** | 错题次数 ≥ 2 | 错题次数 ↓ | 优先攻克 |
| **待复习** | 掌握状态 = 待复习 | 上次错误日期 ↑ | 复习队列 |

---

## 六、开发路线图（优化版）

### 🎯 核心策略：后端优先，逐层验证

```
Notion Database 准备（你先弄） → 后端 API（先跑通核心逻辑）
    → 前端页面（套上去） → 部署（小步快跑）
```

每一层独立可验证，不攒堆。

---

### Step 0：准备工作（你先搞定，10 分钟）

在 Notion 里新建一个页面，输入 `/table` 选择 **Table - Inline**，创建好一个空表格。
然后把页面链接发给我，我来帮你配好所有列（模块/题型/错误原因/知识点…）。

**你不需要自己配列，你只需要：**
1. 在 Notion 新建一个空白页面
2. 输入 `/table` 回车
3. 把页面链接发给我

---

### Step 1：后端核心 — 调 Claude 识别图片（半天）

**做什么：**
- 搭一个 Express 服务，监听某个端口
- 实现 `POST /api/process`：接收图片 → 调 Claude Vision → 返回结构化 JSON
- Prompt 已预设在文档第七章，直接拿来用

**怎么验证：**
```bash
# 用一张错题截图测试，不用打开浏览器
curl -X POST http://localhost:3000/api/process \
  -F "image=@错题截图.jpg"
# 如果返回 JSON 就是成功了
```

**产出物：** 一个可以在终端里测试的 API，发图片进去，出结构化 JSON。

---

### Step 2：Notion 写入（半天）

**做什么：**
- 实现 `POST /api/sync-to-notion`：接收结构化数据 → 在 Notion Database 创建一条记录
- 包括：填充属性（模块/题型/错误原因等）+ 添加内容区（原题/选项/解析）

**怎么验证：**
```bash
curl -X POST http://localhost:3000/api/sync-to-notion \
  -H "Content-Type: application/json" \
  -d '{"module":"判断推理","questionType":"图形推理",...}'
# 去 Notion 里看多了一行就是成功了
```

**产出物：** API 调通后，Notion 里能看到一条完整错题记录。

---

### Step 3：前端页面（一天）

**做什么：**
- 一个 HTML 页面，放 `server/public/` 下由 Express 托管
- 三个区域：
  1. **上传区** — 拍照 `<input capture="environment">` + 拖拽 + 选图
  2. **预览+编辑区** — 展示识别结果，下拉框/文本框可修改
  3. **同步按钮** — 点击写到 Notion

**怎么验证：**
- Mac 浏览器打开 `http://localhost:3000` 测试
- 同一 WiFi 下手机打开 `http://你Mac的IP:3000` 拍照测试

**产出物：** 完整的 PWA 页面，手机打开 → 拍照 → 出结果 → 同步到 Notion。

---

### Step 4：部署上线（半天）

**做什么：**
- 把 `server/` 打包部署到 `39.96.52.165`
- 配置环境变量（API Key / Notion Token / Database ID）
- PM2 管理进程

**怎么验证：**
- 手机浏览器访问 `http://39.96.52.165:端口号`
- 实际拍照走一遍全流程

---

### Phase 2：批量 + 分类管理（后期）

- 一次传多张图排队处理
- 历史记录列表（从 Notion 拉取展示）
- 错题去重（相同题目自动累加错题次数）
- 图片存云（R2/OSS），Notion 里可回看原图

### Phase 3：复盘辅助（后期）

- 薄弱知识点统计（按模块/错误原因聚合）
- 复习优先级推荐（高频错题优先）
- 按「错误原因」生成专题练习建议

### Phase 4：体验进阶（可选）

- 微信小程序（拍照更方便）
- 批量从粉笔/华图 App 截图后自动识别
- 错题回顾的间隔提醒
- 多用户支持，部署供考友使用

---

## 七、核心 Prompt 设计

这是整个系统的灵魂。需要根据考公题型反复迭代。

### V1：带预设分类的稳定版（推荐起点）

Prompt 中嵌入完整的分类预设表，LLM 优先从枚举值中选取，大幅降低输出不稳定性。

```system
你是一个考公行测错题整理助手。用户会发来一张错题的截图或照片，
请识别并提取以下信息，严格按照给定的枚举值选取分类，以 JSON 格式返回：

{
  "module": "",           // 必填，从下方枚举选
  "questionType": "",     // 必填，从下方枚举选
  "errorReason": "",      // 必填，从下方枚举选
  "knowledgePoints": [],  // 必填，优先从下方预设中选，可多选，如无匹配可新增
  "source": "",           // 选填，常见: 粉笔/华图/花生十三/中公/历年真题/其他
  "question": "",         // 必填，题目完整文本
  "options": { "A": "", "B": "", "C": "", "D": "" },
  "myAnswer": "",         // 用户的错误答案
  "correctAnswer": "",    // 正确答案
  "solution": ""          // 分步骤的解题思路，便于复习时理解
}

========== 枚举值定义（请严格选取） ==========

### module（模块）
判断推理 | 言语理解 | 数量关系 | 资料分析 | 常识判断

### questionType（题型，按模块细分）

判断推理:
  图形推理 | 定义判断 | 类比推理 | 逻辑判断

  图形推理细分类（选填，填入 knowledgePoints）:
    位置规律（平移/旋转/翻转）
    样式规律（遍历/叠加/黑白运算/求同存异）
    属性规律（对称/曲直/开闭）
    数量规律（面/线/点/角/元素/笔画）

  逻辑判断细分类（选填，填入 knowledgePoints）:
    形式推理（翻译推理/真假推理/排列组合/集合推理）
    归因论证（另有他因/因果倒置/否定此因）
    类比论证
    一般质疑（直接反驳/拆桥）
    方案论证（可行性/副作用）
    前提假设（搭桥/必要条件）
    推出型

言语理解:
  逻辑填空 | 片段阅读 | 语句表达 | 篇章阅读

  逻辑填空细分类（选填，填入 knowledgePoints）:
    语境分析（转折/递进/并列/解释说明/因果关系）
    词语辨析（实词/成语/关联词）

  片段阅读细分类（选填，填入 knowledgePoints）:
    主旨概括 | 意图判断 | 细节理解 | 词句理解

  语句表达细分类（选填，填入 knowledgePoints）:
    语句排序 | 语句填空

数量关系:
  数学运算 | 数字推理

  数学运算细分类（选填，填入 knowledgePoints）:
    工程问题/行程问题/经济利润/排列组合/概率问题
    容斥问题/几何问题/最值问题/溶液问题/年龄问题
    牛吃草/植树问题/方阵问题/周期问题/钟表问题

资料分析:
  （题型统称为资料分析，细分类填入 knowledgePoints）

  概念类:
    增长率（普通/间隔/混合/年均）
    比重（现期/基期/比重变化）
    增长量 | 倍数与平均数

  速算技巧类:
    直除法 | 特征数字法 | 分数比较法 | 估算（截位/放缩）
    假设分配法 | 415法

  做题策略类:
    读题圈圈法 | 常见陷阱（时间/单位/概念）

常识判断:
  常识别按细分类（填入 knowledgePoints）:
    法律/科技/文史/地理/经济/时政/管理

### errorReason（错误原因，必选其一）
知识点盲区 | 审题失误 | 掉坑 | 计算错误 | 时间不够 | 思路卡壳

### knowledgePoints（知识点标签）
优先从以上各模块的细分类中选取最匹配的标签，可多选。
如以上列表中没有匹配项，允许自行补充新标签。

========== 输出要求 ==========

1. 所有字段都尽量填写，只有 genuinely 无法识别的字段才填 null
2. 如果图片中有手写的错误标记（打勾/画圈/涂改），以用户标记为 myAnswer
3. 如果图片是答案解析截图而非原题，优先提取知识点和解题思路
4. module / questionType / errorReason 必须从枚举值中选，不要自创
5. solution 写 2-4 步，模拟考场思考过程，可用「先……再……」句式
```

### V2 迭代方向：按花生体系细分 Prompt

当前的统一 Prompt 对所有题型使用同一套指令，后续可以按模块拆分 Prompt：

- **判断推理专用 Prompt**：强化论证模型识别（归因/类比/方案…）
- **资料分析专用 Prompt**：强化速算方法识别（假设分配/415 法…），弱化解题步骤文字量
- **言语理解专用 Prompt**：强化语境关系识别（转折/递进/并列…）
- **数量关系专用 Prompt**：强化题型归类（工程/行程/排列组合…）

此外：
- 如果能从图片中识别出「刷题 App 的水印」，自动填写来源字段（粉笔、华图、花生十三等）
- 对于手写笔记类图片，降低结构化要求，更多地直接提取文字内容
- 随着使用积累新知识点标签，定期回填到预设列表中

---

## 八、目录结构（推荐）

```
examTools/                  # 项目根目录（也是 git 仓库）
├── server/                 # Node.js 后端（部署时打包这个目录）
│   ├── server.js           # Express 入口 + 静态文件托管
│   ├── routes/
│   │   └── process.js      # 图片处理 + Notion 写入路由
│   ├── services/
│   │   ├── llm.js          # Claude API 调用（Vision + 结构化）
│   │   └── notion.js       # Notion API 操作（创建 Page）
│   ├── prompts/
│   │   └── extract.js      # 结构化 Prompt（含完整预设枚举）
│   ├── utils/
│   │   └── image.js        # 图片预处理（方向纠正、压缩）
│   ├── public/             # 前端静态文件（Express 托管）
│   │   ├── index.html      # 主页面
│   │   ├── app.js          # 前端逻辑
│   │   └── style.css       # 样式
│   ├── package.json
│   └── .env.example        # 环境变量模板
├── docs/
│   └── 方案文档.md          # 本文件
├── .gitignore
└── README.md
```

---

## 九、关键风险 & 应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| 拍照质量差（反光/模糊/歪斜） | 高 | 识别率下降 | 前端提示用户重新拍摄；内置基础图像预处理（OpenCV 校正） |
| 手写文字识别不准 | 中 | 部分字段丢失 | LLM 本身对上下文有理解能力；关键字段允许用户编辑 |
| LLM API 费用 | 中 | 使用成本 | 单张约 ~$0.01-0.03 (Claude)；个人每月 $5-10 足够 |
| Notion API 限频 | 低 | 批量写入失败 | 批量时加间隔；错误重试机制 |
| 同一道题重复录入 | 中 | Notion 出现重复 Page | 基于题目摘要的相似度匹配去重；自动累加错题次数 |

---

## 十、开发检查清单

### Phase 1 MVP

- [ ] 注册 / 配置 LLM API Key（Claude）
- [ ] 创建 Notion Integration 并获取 Token
- [ ] 在 Notion 创建目标 Database 并记录 Database ID
- [ ] 搭建后端项目，实现 `/api/process` 接口
  - [ ] 接收图片 → 转 base64 → 调 Claude Vision
  - [ ] 解析 JSON 返回
- [ ] 实现 Notion 写入服务
  - [ ] 创建 Page（填充属性）
  - [ ] 添加内容 Block（图片 + 文本 + 选项 + 解析）
- [ ] 搭建前端页面
  - [ ] 图片上传区域（拖拽 + 点击）
  - [ ] 图片预览
  - [ ] 结构化结果展示 + 编辑
  - [ ] 同步到 Notion 按钮
- [ ] 端到端联调

### Phase 2

- [ ] 图片云存储（R2 / OSS）
- [ ] 错题历史列表（本地缓存 or 从 Notion 拉取）
- [ ] 批量上传功能
- [ ] 去重逻辑（题目文本相似度比较）

### Phase 3

- [ ] 统计数据接口（按模块 / 错误原因 / 知识点聚合）
- [ ] 薄弱点可视化（简单图表）
- [ ] 复习优先级排序逻辑

---

## 十一、后续可能的扩展

- **微信小程序**：利用系统相册 + 拍照更方便
- **间隔复习提醒**：根据遗忘曲线推送需要复习的题目
- **智能组卷**：从高频错题中自动生成模拟卷
- **粉笔/华图数据打通**：导出 App 收藏夹，批量结构化
- **打印输出**：生成「错题本 PDF」用于纸质复习

---

> **开始开发参考**：如果从 Phase 1 开始，建议先配好 Notion 和 Claude 的 API，然后按照「后端处理接口 → Notion 写入 → 前端页面」的顺序推进。遇到具体问题时欢迎继续讨论。

---

## 十二、部署与运维

### 12.1 部署流程

服务器地址：`39.96.52.165`，项目以 `imgtools` 为 PM2 进程名管理。

**首次部署（或全量更新）：**

```bash
# 1. 本地打包（排除 node_modules 和上传目录）
tar --exclude='node_modules' --exclude='uploads' -czf /tmp/server.tar.gz server/

# 2. 上传到服务器
scp /tmp/server.tar.gz root@39.96.52.165:/opt/

# 3. 登录服务器，解压并重启
ssh root@39.96.52.165
cd /opt/server && tar xzf /tmp/server.tar.gz
pm2 restart imgtools
```

### 12.2 Git 工作流

**设置代理（如果连不上 GitHub）：**

```bash
git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897
```

**常规提交流程：**

```bash
git add -A
git commit -m "改了什么内容"
git push
```

**推送完后清除代理：**

```bash
git config --global --unset http.proxy
git config --global --unset https.proxy
```

### 12.3 项目地址

> GitHub 仓库地址：待补充（用户告知后填入）
